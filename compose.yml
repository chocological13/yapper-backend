services:
  db:
    image: postgres:alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: yapper
      POSTGRES_PASSWORD: passw0rd
    # volumes:
    #   - db_data:/var/lib/postgresql/data
# volumes:
# db_data:
