version: "2"
sql:
  - engine: "postgresql"
    queries: "pkg/database/query"
    schema: "/migrations"
    gen:
      go:
        package: "repository"
        out: "pkg/database/repository"
        sql_package: "pgx/v5"
        emit_interface: true
